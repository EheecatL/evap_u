{% extends '@App/base/index.html.twig' %}

{% block title %}

    Accessoires

{% endblock %}

{% block content %}

    <a href="{{ path('products') }}" class="return">
        <div class="container-fluid">
            <div class="row mb-3">

                <div class="ml-3">
                    <i class="fas fa-arrow-left"></i>
                </div>
                <div class="ml-2 align-self-center responsivereturn">
                    Retour
                </div>
            </div>
        </div>
    </a>


    <div class="container-fluid mb-5">
        <div class="row">
            <div class="col-lg-3 col-sm-6 text-center titres">Accessoires</div>
        </div>
    </div>

    <div class="container mb-5 text-justify">
        <p>
            Il existe une multitude d'accessoires. Que ce soit des accus pour une boxe mécanique, ou des chargeurs pour une boxe électronique, chaque accessoire de vape peut-être différent. Il existe aussi des cotons, des fils résistifs, etc... Ils peuvent aussi se décliner sous forme d'étui, d'outillage ou même de pièces de remplacement pour une utilisation plus poussée des connaisseurs.
        </p>
    </div>

    <div class="row mb-5">

        {% for product in product %}

            <div class="col-lg-4 text-center">
                <img src="{{ asset('uploads/img/'~product.image) }}" width="50%">
                <h4 class="mb-5">{{ product.name }}</h4>
                <p class="text-justify offset-1 col-10 mb-5">{{ product.content }}</p>
            </div>

        {% endfor %}





    </div>


{% endblock %}