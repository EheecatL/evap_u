{% extends '@App/base/index.html.twig' %}

{% block title %}

    Clearomiseurs

{% endblock %}

{% block content %}

    <a href="{{ path('products') }}" class="return">
        <div class="row mb-3">

            <div class="ml-5">
                <i class="fas fa-arrow-left"></i>
            </div>
            <div class="ml-2 align-self-center">
                Retour
            </div>
        </div>
    </a>

    <div class="container-fluid mb-5">
        <div class="row">
            <div class="col-3 text-center titres">Atomiseurs</div>
        </div>
    </div>

    <div class="container mb-5">
        <p>
            Le clearomiseur, ou atomiseur est un élement central dans la cigarette électronique. Un clearomiseur est composé de deux éléments principaux: un réservoir, ou tank, qui contient le e liquide, et un système de vaporisation, lequel permet de chauffer le e liquide, de créer la vapeur et de l'amener à votre bouche.
        </p>
    </div>

    <div class="row mb-5">

    {% for product in product %}

        <div class="col-4 text-center">
            <img src="{{ asset('uploads/img/'~product.image) }}" width="50%">
            <h4 class="mb-5">{{ product.name }}</h4>
            <p class="text-justify offset-1 col-10">{{ product.content }}</p>
        </div>

    {% endfor %}





        </div>


{% endblock %}